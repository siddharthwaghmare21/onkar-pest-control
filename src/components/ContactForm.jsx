"use client";
import { useState } from "react";
const api = process.env.NEXT_PUBLIC_API_URL || "http://localhost:5157";
export default function ContactForm(){const [state,setState]=useState("idle");async function submit(e){e.preventDefault();setState("sending");const body=Object.fromEntries(new FormData(e.currentTarget).entries());try{const r=await fetch(`${api}/api/contact-messages`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok)throw new Error();e.currentTarget.reset();setState("success")}catch{setState("error")}}return <form className="booking-form" onSubmit={submit}><label>Name<input name="name" required placeholder="Your name"/></label><label>Phone<input name="phone" required type="tel" pattern="[0-9]{10}" placeholder="10-digit mobile number"/></label><label>Email <span>(optional)</span><input name="email" type="email" placeholder="you@example.com"/></label><label>Subject<input name="subject" required placeholder="How can we help?"/></label><label>Message<textarea name="message" required placeholder="Tell us how we can help"/></label><button className="button button-primary" disabled={state==="sending"}>{state==="sending"?"Sending…":"Send Message →"}</button>{state==="success"&&<p role="status">Thank you! Your message was received.</p>}{state==="error"&&<p role="alert">Something went wrong. Please call us directly.</p>}</form>}
