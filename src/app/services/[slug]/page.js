import {notFound} from "next/navigation"; import Link from "next/link"; import SiteHeader from "@/components/SiteHeader"; import SiteFooter from "@/components/SiteFooter"; import PageHero from "@/components/PageHero"; import FloatingActions from "@/components/FloatingActions"; import {getService} from "@/data/services";
export default async function ServiceDetail({params}){const {slug}=await params;const service=getService(slug);if(!service)notFound();const I=service.icon;return <><SiteHeader/><PageHero eyebrow="SERVICE DETAIL" title={<>{service.nameEn}<br/><em>{service.nameMr}</em></>} copy={service.summary}/><main className="container detail-content service-detail"><div className="detail-icon"><I size={32}/></div><h2>A considered approach to pest control.</h2><p>{service.summary} आम्ही जागेची स्थिती आणि problem समजून घेऊन योग्य service coordination करतो.</p><div className="detail-cta"><Link className="button button-primary" href="/book-service">Book a Service →</Link><a className="text-link" href="tel:8788300933">Call 8788300933</a></div></main><SiteFooter/><FloatingActions/></>}
